# Minimal runnable agent: Orchestrator + OpenAI LM + reply (egress)
actions:
  - action: jvagent/orchestrator
    context:
      enabled: true
      model: gpt-4o-mini
      model_action_type: OpenAILanguageModelAction
      model_temperature: 0.2
      activation_budget: 24
      history_limit: 4
      lock_active_flow: true
      tool_servers: "-all"
      skills_source: both
      skills: "-all"
      clarify_text: "Sorry, I didn't quite catch that — could you rephrase?"

  - action: jvagent/openai_lm
    context:
      enabled: true
      description: OpenAI language model (OPENAI_API_KEY in .env)
      api_endpoint: https://api.openai.com/v1
      model: gpt-4o-mini
      temperature: 0.2
      max_tokens: 4096
      timeout: 60

  - action: jvagent/reply
    context:
      enabled: true
      description: Egress voice — furnishes the reply/respond tools and voices replies
      model_action_type: OpenAILanguageModelAction
      model: gpt-4o-mini
      model_temperature: 0.1
      model_max_tokens: 8192
