# knowledge_ingest — library JV skill

Reusable capture → report → PageIndex assimilate SOP. Discovered when
`skills_source` includes `library`/`both` and the agent's `skills` list
includes `knowledge_ingest` or `-all`. Requires `WebFetchAction` +
`PageIndexAction` on the agent.
