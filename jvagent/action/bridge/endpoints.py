"""HTTP endpoints for ``BridgeInteractAction``.

No endpoints are registered at milestone B — Bridge introspection routes are
added in milestone I (observability). This module exists so the package
follows the four-file action layout and ``from . import endpoints`` in
``__init__.py`` does not fail.
"""
