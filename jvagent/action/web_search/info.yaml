package:
  name: jvagent/web_search
  author: V75 Inc.
  archetype: BaseWebSearchAction
  version: 1.0.0
  meta:
    title: Web Search Action
    description: >
      Abstract base action for web search providers (SerpAPI, Brave, etc.).
      Provider pip dependencies are declared in each provider's own info.yaml.
    group: jvagent
    type: web_search_action
  config:
    order:
      weight: 0
  dependencies:
    jvagent: ~0.0.1
