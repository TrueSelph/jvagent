"""HTTP endpoints for ``ReflexHelm``.

No endpoints at E — observability routes (per-helm timing, classification
trace) arrive at milestone I. Stub kept so the four-file action layout
holds and ``from . import endpoints`` does not fail.
"""
