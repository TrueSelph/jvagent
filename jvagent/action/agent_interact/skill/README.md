# `jvagent.action.agent_interact.skill`

AgentInteract-local skill loop harness (subclasses platform `SkillAction`), visitor shim, hot reload, run config mapping, **native tools** (`converse_skill`, always-on `list_skills` / `skill_search` / `plan_skills`), and **conversational delivery** shared with the conversational gate.

See [AgentInteract README](../README.md) and [docs/agent-interact.md](../../../../docs/agent-interact.md).
