# RetrievalInteractAction Package Descriptor
# This file defines the action's package metadata, dependencies, and configuration

package:
  # Action name in namespace/action_name format
  name: jvagent/gather_info_interact_action

  # Package author
  author: jvagent

  # Archetype: The main Action class name (same as the Action Node class)
  archetype: GatherInfoInteractAction

  # Package version
  version: 1.0.0

  # Package metadata
  meta:
    title: GatherInfoInteractAction
    description: GatherInfoInteractAction that goes through a series of questions to get the user's information
    group: jvagent
    type: interact_action

  # Package configuration
  config:
    order:
      weight: -40

  # Package dependencies
  dependencies:
    # jvagent version requirement
    jvagent: ~0.0.1
    # Other action dependencies (by namespace/action_name)
    actions: []
