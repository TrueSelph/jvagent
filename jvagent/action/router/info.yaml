# InteractRouter Action Package Descriptor
# This file defines the action's package metadata, dependencies, and configuration

package:
  # Action name in namespace/action_name format
  name: jvagent/interact_router
  
  # Package author
  author: jvagent
  
  # Archetype: The main Action class name (same as the Action Node class)
  archetype: InteractRouter
  
  # Package version
  version: 0.0.1
  
  # Package metadata
  meta:
    title: InteractRouter
    description: Intent-based routing action that analyzes utterances and routes to appropriate InteractActions
    group: jvagent
    type: interact_action
  
  # Package configuration
  config:
    order:
      weight: -100
  
  # Package dependencies
  dependencies:
    # jvagent version requirement
    jvagent: ~0.0.1
    # Other action dependencies (by namespace/action_name)
    actions: []

