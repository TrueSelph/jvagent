# Once-over review — 2026-07-17

Follow-on to [2026-07-16-core-review.md](2026-07-16-core-review.md). Four parallel reviewers swept surfaces the core review skipped (egress/channels, logging, response bus, orchestrator resilience, multi-worker). Findings spot-verified; remediations landed in batches **S1–S5**.

## Severity corrections (vs initial synthesis)

| Claim | Initial | Verified | Note |
|---|---|---|---|
| Email send no admin | HIGH | **HIGH** | Confirmed |
| Inbound From / no SPF-DKIM | HIGH | **HIGH** | Confirmed (SendGrid form fields) |
| Log retention dead + base64 logs | HIGH | **HIGH** | Confirmed |
| ResponseBus on cached Agent | HIGH | **HIGH** | TTL dict, not LRU; registry fix |
| Empty enum cancels flows | HIGH | **MED** | Empty `names` does not cancel owned tasks; **partial** non-empty enum + enum-failure flag is the real hazard |
| Interview branch prune-all | MED | **MED** | Missing hook pruned unreachable path, not literally all answers |

## Clean (left alone)

Memory admin authz, session-token HMAC/rotation, Meta signature verify, OAuth single-use state, webui path-traversal guard, `--purge` gate, loop bound enforcement, plan-resume ADR-0019, MCP tool naming.

## Remediation batches

| Batch | Scope | Status |
|---|---|---|
| **S1** | Egress authz: email admin, SendGrid SPF/DKIM, reply `user_id` bind, WhatsApp QR admin | Done |
| **S2** | Log retention purge, media/base64 redaction, `_events` cap, `terminal_ttl_days` default 30 | Done |
| **S3** | Enum-failure skip orphan cancel, locked-flow timeout, observation cap, branch-hook fail-turn, MCP connect lock, directive trust | Done |
| **S4** | Process-level ResponseBus registry, SSE `id` dedup, idle accumulator/session eviction | Done |
| **S5** | User RMW lock+reload, proactive CAS, Messenger dedup, `interaction_limit=0`, uninstall confirm, source agent prune, pip safety, enable/disable cache, Dynamo log AWS env | Done |

## Key code anchors (post-fix)

- Email admin: `jvagent/action/email_action/endpoints.py` (`email_send` `roles=["admin"]`)
- Inbound auth: `jvagent/action/email_action/inbound/auth.py`
- Log retention: `jvagent/logging/retention.py` + `TaskMonitor.tick`
- Bus registry: `jvagent/action/response/response_bus.py` `get_agent_response_bus`
- Orphan skip: `OrchestratorInteractAction._actions_enum_failed` + `loop.py`
- Agent uninstall: `jvagent/cli/agent_commands.py` `--yes` / type `yes`

## Follow-ups (not in this pass)

- Redis/Dynamo-backed Meta mid dedup for multi-worker (in-process store remains)
- Full directive allowlist ADR if contrib surface grows
- Distributed lock required when `--workers>1` (document + optional hard fail)
